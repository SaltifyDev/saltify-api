package org.ntqqrev.saltify.api.context.message

/**
 * Represents how the image appears in the chat window.
 */
enum class ImageSubType {
    /**
     * The image is a normal image.
     */
    NORMAL,

    /**
     * The image is a sticker.
     */
    STICKER,
}